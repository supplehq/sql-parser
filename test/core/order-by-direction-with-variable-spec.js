import * as tree from '../misc/helpers';

describe('select', function() {

  it('basic order by direction', function(done) {
    tree.equals(this, done);
  });

  it('order by direction with variable', function(done) {
    tree.equals(this, done);
  });

});
