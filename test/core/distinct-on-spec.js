import * as tree from '../misc/helpers';

describe('select', function() {

  it('select distinct', function(done) {
    tree.equals(this, done);
  });

  it('select distinct on', function(done) {
    tree.equals(this, done);
  });

});
